{
	"name": "Hugo Development",
	"image": "mcr.microsoft.com/devcontainers/base:ubuntu",

	"features": {
		"ghcr.io/devcontainers/features/hugo:1": {
			"extended": true,
			"version": "latest"
		}
	},

	"postCreateCommand": "git submodule update --init --recursive && hugo version && echo 'alias hugodev=\"hugo server -D --appendPort=false --baseURL https://\\$CODESPACE_NAME-1313.\\$GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN\"' >> ~/.bashrc",

	"forwardPorts": [1313],

	"customizations": {
		"vscode": {
			"extensions": [
				"budparr.language-hugo-vscode",
				"eliostruyf.vscode-hugo-themer"
			]
		}
	}
}
