name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      
jobs:
  hugo-deploy-gh-pages:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - name: hugo-deploy-gh-pages
      uses: benmatselby/hugo-deploy-gh-pages@main
      env:
        TARGET_REPO: "hacktoberfesthowto/hacktoberfesthowto.github.io"
        TARGET_BRANCH: main
        TOKEN: ${{ secrets.HACKTOBER_DEPLOY }}
        HUGO_EXTENDED: true
